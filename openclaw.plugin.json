{
  "id": "hirey",
  "name": "Hirey AI",
  "description": "Hirey Hi for OpenClaw — register Hi's tools, agent-events claim service, and webhook ingress in-process. People-to-people matching for jobs, hiring, housing, friendship, dating, founders, investors, lawyers, and any other human-lead goal.",
  "activation": {
    "onStartup": true,
    "onCapabilities": [
      "tool",
      "hook"
    ],
    "onCommands": []
  },
  "skills": [
    "./skills"
  ],
  "contracts": {
    "tools": [
      "hi_agent_status",
      "hi_agent_install",
      "hi_agent_doctor",
      "hi_agent_reset",
      "hi_agent_recover",
      "hi_agent_state_resync",
      "hi_agent_claim_export",
      "hi_agent_claim_redeem",
      "hi_pull_events",
      "email_binding",
      "google_link",
      "phone_binding",
      "workspace_workflows"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "platformBaseUrl": {
        "type": "string",
        "description": "Hirey Hi platform base URL. Defaults to the Hirey AI public service.",
        "default": "https://hi.hirey.ai"
      },
      "profile": {
        "type": "string",
        "description": "Hi MCP profile name. Used to namespace persisted identity / receiver state on disk.",
        "default": "openclaw-main"
      },
      "stateDir": {
        "type": "string",
        "description": "Filesystem dir for persisted hi identity / receiver runtime cursor. Default ~/.openclaw/hi-mcp/<profile>.",
        "default": ""
      },
      "webhookPath": {
        "type": "string",
        "description": "Plugin-owned HTTP path on the gateway where the in-process receiver service delivers Hi events back into the current LLM session. Default /hi/webhook.",
        "default": "/hi/webhook"
      },
      "claimPollIntervalMs": {
        "type": "integer",
        "description": "How often the in-process agent-events claim loop polls the platform when the long-poll connection is idle.",
        "default": 1500,
        "minimum": 250
      },
      "claimLeaseMs": {
        "type": "integer",
        "description": "Per-claim lease length the loop requests from the platform.",
        "default": 60000,
        "minimum": 1000
      }
    }
  },
  "uiHints": {
    "platformBaseUrl": {
      "label": "Platform base URL",
      "placeholder": "https://hi.hirey.ai"
    },
    "profile": {
      "label": "Hi MCP profile",
      "placeholder": "openclaw-main"
    }
  }
}
